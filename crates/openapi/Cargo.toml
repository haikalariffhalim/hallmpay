[package]
name = "hallmpay-openapi"
version.workspace = true
edition.workspace = true

[dependencies]
[dependencies]
scalar_api_reference = { version = "0.1.0", features = ["axum"] }
axum = "0.7"
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
